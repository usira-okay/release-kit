{
    "configurations": [
        {
            "name": ".NET Core Launch (console)",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "dotnet: build",
            "program": "${workspaceFolder}/src/ReleaseKit.Console/bin/Debug/net9.0/ReleaseKit.Console.dll",
            "args": [
                // "fetch-gitlab-pr"
                // "fetch-bitbucket-pr"
                // "fetch-gitlab-release-branch"
                // "fetch-bitbucket-release-branch"
                // "filter-gitlab-pr-by-user"
                "filter-bitbucket-pr-by-user"
            ],
            "cwd": "${workspaceFolder}/src/ReleaseKit.Console",
            "stopAtEntry": false,
            "console": "internalConsole"
        }
    ]
}